schema: https://graphql.datocms.com/preview
headers:
  Authorization: 'Bearer %{process.env.DATO_API_TOKEN}'
overwrite: true
generates:
  ./lib/fragmentTypes.json:
    plugins:
      - fragment-matcher
